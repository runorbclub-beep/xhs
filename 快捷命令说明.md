#!/bin/bash
# 快捷启动脚本说明

# 1. 启动交互式浏览会话 ⭐（最常用）
./xhs.sh --start-session

# 2. 搜索账号
./xhs.sh --search "牙医" --auto-add

# 3. 生成每日报告
./xhs.sh --report

# 4. 查看监控列表
./xhs.sh --list

# 5. 添加笔记
./xhs.sh --add-note "账号名,笔记ID,标题,URL"

# 6. 查看帮助
./xhs.sh --help
